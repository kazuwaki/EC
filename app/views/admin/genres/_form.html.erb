<%= form_with model:[:admin, @genre], local:true do |f| %>
<table class="table">
  <tbody>
    <tr>
      <td>ジャンル名</td>
      <td><%= f.text_field :name %></td>
      <td><%= f.submit %></td>
    </tr>
  </tbody>
</table>
<% end %>